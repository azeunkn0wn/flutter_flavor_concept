name: flutter_flavor_concept
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_dotenv: ^5.1.0
  package_info_plus: ^4.0.2

dev_dependencies:
  build_runner: ^2.4.5
  flutter_flavorizr: ^2.2.1
  flutter_launcher_icons: ^0.13.1

  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # TODO: A widget might contain a flavor specific image like Logo.
  assets:
    - env/meetyoda/
    - env/myvcmo/
    - assets/images/
